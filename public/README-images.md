# Drop these 3 images here

Save the 3 reference images you sent me into this `public/` folder with these exact filenames:

- `gym-bg.jpg` — the blue gym interior photo (used as full-bleed background, blurred + dimmed)
- `phone-ui.png` — the tilted iPhone "Starting Now" mockup (used inside the glass card)
- `app-icon.png` — the GymSync app icon (used as the brand mark)

The code already references these paths. Once dropped, the page will render correctly.
